Zuul frontend (minimal, Vue + Vite)

This is a minimal Vite + Vue frontend skeleton that proxies `/api` to `http://localhost:8080` for development.

Commands:
  npm install
  npm run dev

The page will attempt to call GET /api/game and display the response.


